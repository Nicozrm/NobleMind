<!DOCTYPE html>
<html lang="de">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
<title>NOBLEmind</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Outfit:wght@200;300;400;500;600&family=JetBrains+Mono:wght@300;400;500&display=swap" rel="stylesheet">
<script src="https://cdnjs.cloudflare.com/ajax/libs/marked/12.0.2/marked.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/dompurify/3.1.5/purify.min.js"></script>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/styles/atom-one-dark.min.css">
<script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/highlight.min.js"></script>
index.html<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/KaTeX/0.16.10/katex.min.css">
<script src="https://cdnjs.cloudflare.com/ajax/libs/KaTeX/0.16.10/katex.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/KaTeX/0.16.10/contrib/auto-render.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/mermaid/10.9.1/mermaid.min.js"></script>
<style>
/* ═══ NOBLEmind v5 · technisch, dunkel, direkt ═══ */
:root{
  --bg:#000; --s1:rgba(255,255,255,.035); --s2:rgba(255,255,255,.06); --s3:rgba(255,255,255,.09);
  --bd:rgba(255,255,255,.09); --bd2:rgba(255,255,255,.15);
  --tx:#F2F2F0; --mu:rgba(242,242,240,.58); --fa:rgba(242,242,240,.34);
  --ac:#C9A962; --ac2:#E8D5A3; --acs:rgba(201,169,98,.12);
  --dg:#e08585; --ok:#8fbf88;
  --r:24px; --r2:14px; --r3:10px;
  --f:'Outfit',system-ui,-apple-system,sans-serif; --fm:'JetBrains Mono',monospace;
  --e:cubic-bezier(.2,.85,.3,1);
}
*{margin:0;padding:0;box-sizing:border-box}
html,body{height:100%}
body{background:var(--bg);color:var(--tx);font-family:var(--f);font-weight:400;overflow:hidden;-webkit-font-smoothing:antialiased}
::selection{background:rgba(201,169,98,.26)}
::-webkit-scrollbar{width:8px;height:8px}
::-webkit-scrollbar-track{background:transparent}
::-webkit-scrollbar-thumb{background:rgba(255,255,255,.1);border-radius:8px;border:2px solid transparent;background-clip:content-box}
::-webkit-scrollbar-thumb:hover{background:rgba(255,255,255,.2);background-clip:content-box}
#glow{position:fixed;inset:0;z-index:0;pointer-events:none;
  background:radial-gradient(70% 50% at 50% -10%,rgba(201,169,98,.055),transparent 70%),
             radial-gradient(50% 40% at 90% 100%,rgba(201,169,98,.028),transparent 70%)}
#liquid{position:fixed;inset:0;width:100%;height:100%;z-index:0;opacity:.5}
#app{position:fixed;inset:0;z-index:2;display:flex}
.ico{width:18px;height:18px;stroke:currentColor;fill:none;stroke-width:1.6;stroke-linecap:round;stroke-linejoin:round;display:block;flex:none}
/* ── SIDEBAR ── */
#rail{width:60px;flex:none;display:flex;flex-direction:column;padding:12px 10px;gap:6px;
  border-right:1px solid var(--bd);background:rgba(255,255,255,.014);
  transition:width .3s var(--e);overflow:hidden;position:relative;z-index:5}
#rail:hover,#rail.pin{width:252px}
.rb{display:flex;align-items:center;gap:12px;height:40px;padding:0 11px;border-radius:var(--r3);
  border:none;background:none;color:var(--mu);cursor:pointer;transition:all .18s;width:100%;flex:none;
  font-family:var(--f);font-size:13.5px;font-weight:400;white-space:nowrap;text-align:left}
.rb:hover{background:var(--s2);color:var(--tx)}
.rb.on{background:var(--s2);color:var(--tx)}
.rb span{opacity:0;transition:opacity .2s;flex:1;overflow:hidden;text-overflow:ellipsis}
#rail:hover .rb span,#rail.pin .rb span{opacity:1}
#logo{color:var(--ac);font-size:19px;line-height:1;display:grid;place-items:center;width:18px;height:18px;flex:none}
#railHead{display:flex;align-items:center;gap:12px;height:40px;padding:0 11px;margin-bottom:6px;flex:none}
#railHead b{font-size:14px;font-weight:600;letter-spacing:.02em;opacity:0;transition:opacity .2s;white-space:nowrap}
#rail:hover #railHead b,#rail.pin #railHead b{opacity:1}
#railHead b i{color:var(--ac);font-style:normal;font-weight:400}
#convList{flex:1;overflow-y:auto;overflow-x:hidden;margin-top:8px;padding-top:8px;border-top:1px solid var(--bd);opacity:0;transition:opacity .2s}
#rail:hover #convList,#rail.pin #convList{opacity:1}
#rail:not(:hover):not(.pin) #convList{pointer-events:none}
.cv{display:flex;align-items:center;gap:8px;padding:8px 11px;border-radius:var(--r3);cursor:pointer;
  color:var(--mu);font-size:13px;transition:all .16s;white-space:nowrap}
.cv:hover{background:var(--s2);color:var(--tx)}
.cv.on{background:var(--s2);color:var(--tx)}
.cv i{flex:1;font-style:normal;overflow:hidden;text-overflow:ellipsis}
.cv button{border:none;background:none;color:var(--fa);cursor:pointer;font-size:13px;opacity:0;padding:0 2px;flex:none;line-height:1}
.cv:hover button{opacity:.8}
.cv button:hover{color:var(--dg)}
.rail-foot{margin-top:auto;padding-top:8px;border-top:1px solid var(--bd);flex:none}
/* ── STAGE ── */
#stage{flex:1;display:flex;flex-direction:column;min-width:0;position:relative}
#topbar{display:flex;align-items:center;gap:10px;padding:14px 20px;flex:none}
.tpill{display:flex;align-items:center;gap:8px;height:34px;padding:0 13px;border-radius:999px;
  border:1px solid var(--bd);background:var(--s1);color:var(--mu);cursor:pointer;
  font-family:var(--f);font-size:12.5px;font-weight:400;transition:all .2s}
.tpill:hover{background:var(--s2);color:var(--tx);border-color:var(--bd2)}
.tpill b{color:var(--tx);font-weight:500}
.tpill .sg{color:var(--ac);font-size:13px;line-height:1}
#topbar .sp{flex:1}
#dot{width:6px;height:6px;border-radius:50%;background:var(--ok);flex:none;margin-right:4px;transition:all .3s}
#dot.work{background:var(--ac);animation:bre 1.1s ease-in-out infinite}
@keyframes bre{0%,100%{opacity:.35}50%{opacity:1;box-shadow:0 0 10px var(--ac)}}
#scroll{flex:1;overflow-y:auto;overflow-x:hidden;display:flex;flex-direction:column}
/* ── LANDING ── */
#landing{flex:1;display:flex;flex-direction:column;align-items:center;justify-content:center;
  padding:20px 20px 60px;gap:26px;animation:up .5s var(--e)}
@keyframes up{from{opacity:0;transform:translateY(14px)}to{opacity:1;transform:none}}
#landing h1{font-size:clamp(26px,3.6vw,38px);font-weight:300;letter-spacing:-.02em;text-align:center;color:var(--tx)}
#landing h1 b{font-weight:500;color:var(--ac)}
#sugg{display:flex;gap:9px;flex-wrap:wrap;justify-content:center;max-width:660px}
.sg-chip{display:flex;align-items:center;gap:8px;height:34px;padding:0 14px;border-radius:999px;
  border:1px solid var(--bd);background:var(--s1);color:var(--mu);cursor:pointer;
  font-family:var(--f);font-size:12.5px;transition:all .2s var(--e)}
.sg-chip:hover{background:var(--s2);color:var(--tx);border-color:var(--bd2);transform:translateY(-1px)}
.sg-chip em{font-style:normal;color:var(--ac);font-size:13px}
/* ── CHAT ── */
#thread{max-width:760px;width:100%;margin:0 auto;padding:12px 20px 24px;display:flex;flex-direction:column;gap:22px}
.msg{animation:up .4s var(--e) both;max-width:100%}
.msg.flash{animation:fl 1.6s var(--e)}
@keyframes fl{0%,100%{background:transparent}30%{background:var(--acs)}}
/* User: Blase rechts, wie bei Grok */
.msg.user{align-self:flex-end;max-width:76%;display:flex;flex-direction:column;align-items:flex-end;gap:7px}
.msg.user .body{background:var(--s3);border:1px solid var(--bd);border-radius:var(--r) var(--r) 5px var(--r);
  padding:12px 17px;font-size:14.5px;line-height:1.65;color:var(--tx)}
.msg.user .attach-strip{justify-content:flex-end}
/* Assistant: volle Breite, keine Blase */
.msg.ai{width:100%}
.ai-head{display:flex;align-items:center;gap:9px;margin-bottom:11px;height:20px}
.ai-sig{width:22px;height:22px;border-radius:7px;background:var(--acs);border:1px solid rgba(201,169,98,.24);
  display:grid;place-items:center;color:var(--ac);font-size:11px;flex:none}
.ai-name{font-size:12.5px;font-weight:500;color:var(--tx)}
.ai-model{font-family:var(--fm);font-size:9.5px;color:var(--fa);background:var(--s1);border:1px solid var(--bd);
  padding:2px 7px;border-radius:5px}
.msg-actions{margin-left:auto;display:flex;gap:2px;opacity:0;transition:opacity .2s}
.msg:hover .msg-actions{opacity:1}
.ma{border:none;background:none;color:var(--fa);cursor:pointer;padding:5px;border-radius:6px;
  display:grid;place-items:center;transition:all .16s}
.ma:hover{background:var(--s2);color:var(--tx)}
.ma.danger:hover{color:var(--dg)}
.ma .ico{width:14px;height:14px;stroke-width:1.7}
.msg.user .msg-actions{margin-left:0}
.body{font-size:15px;line-height:1.72;color:var(--tx);overflow-wrap:break-word;font-weight:400}
.body p{margin:0 0 12px}.body p:last-child{margin:0}
.body h1,.body h2,.body h3{font-weight:600;color:var(--tx);margin:22px 0 10px;line-height:1.3;letter-spacing:-.01em}
.body h1{font-size:21px}.body h2{font-size:18px}.body h3{font-size:16px}
.body ul,.body ol{margin:0 0 12px 20px}.body li{margin:5px 0}
.body li::marker{color:var(--ac)}
.body a{color:var(--ac2);text-decoration:none;border-bottom:1px solid rgba(232,213,163,.3)}
.body a:hover{border-color:var(--ac2)}
.body blockquote{border-left:2px solid var(--ac);padding:2px 0 2px 15px;margin:14px 0;color:var(--mu)}
.body strong{font-weight:600;color:#fff}
.body table{border-collapse:collapse;margin:14px 0;width:100%;font-size:13.5px;
  border:1px solid var(--bd);border-radius:var(--r3);overflow:hidden}
.body th{background:var(--s2);font-size:11px;letter-spacing:.06em;text-transform:uppercase;color:var(--mu);
  text-align:left;padding:9px 13px;font-weight:500}
.body td{padding:9px 13px;border-top:1px solid var(--bd);color:var(--mu)}
.body code:not(pre code){font-family:var(--fm);font-size:.85em;color:var(--ac2);background:var(--s2);
  padding:2px 6px;border-radius:5px;border:1px solid var(--bd)}
.body hr{border:none;border-top:1px solid var(--bd);margin:20px 0}
.body img{max-width:100%;border-radius:var(--r2);margin:10px 0;display:block;border:1px solid var(--bd)}
.body .mermaid-box{background:var(--s1);border:1px solid var(--bd);border-radius:var(--r2);padding:16px;
  margin:14px 0;display:flex;justify-content:center;overflow-x:auto}
.codewrap{margin:14px 0;border:1px solid var(--bd);border-radius:var(--r2);overflow:hidden;background:rgba(255,255,255,.02)}
.code-head{display:flex;align-items:center;gap:8px;padding:8px 13px;background:var(--s1);border-bottom:1px solid var(--bd)}
.code-lang{font-family:var(--fm);font-size:10.5px;color:var(--mu);flex:1}
.code-btn{border:none;background:none;color:var(--fa);font-family:var(--f);font-size:11.5px;cursor:pointer;
  padding:3px 8px;border-radius:6px;transition:all .16s}
.code-btn:hover{background:var(--s2);color:var(--tx)}
.codewrap pre{margin:0;padding:14px 16px;overflow-x:auto;background:transparent!important}
.codewrap pre code{font-family:var(--fm);font-size:12.5px;line-height:1.7;background:transparent!important;font-weight:400}
.attach-strip{display:flex;gap:7px;flex-wrap:wrap;margin-bottom:9px}
.attach-strip img{width:56px;height:56px;object-fit:cover;border-radius:9px;border:1px solid var(--bd)}
.file-tag{display:inline-flex;align-items:center;gap:7px;padding:6px 11px;border-radius:8px;
  border:1px solid var(--bd);background:var(--s1);font-size:11.5px;color:var(--mu)}
/* Werkzeug-Spur */
.tool-flow{display:flex;flex-direction:column;gap:5px;margin-bottom:11px}
.t-chip{display:inline-flex;align-items:center;gap:9px;align-self:flex-start;padding:5px 12px;border-radius:999px;
  border:1px solid var(--bd);background:var(--s1);font-size:11.5px;color:var(--mu);animation:up .3s var(--e);
  max-width:100%;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.t-chip .pulse{width:5px;height:5px;border-radius:50%;background:var(--ac);animation:bre 1.1s infinite;flex:none}
.t-chip.done .pulse{animation:none;background:var(--ok)}
.reason{border:1px solid var(--bd);border-radius:var(--r2);background:var(--s1);margin-bottom:12px;overflow:hidden}
.reason summary{cursor:pointer;padding:9px 14px;font-size:12px;color:var(--mu);list-style:none;
  display:flex;align-items:center;gap:9px;transition:color .2s}
.reason summary::-webkit-details-marker{display:none}
.reason summary::before{content:"";width:5px;height:5px;border-radius:50%;background:var(--ac);flex:none}
.reason summary:hover{color:var(--tx)}
.reason[open] summary{border-bottom:1px solid var(--bd);color:var(--tx)}
.reason .r-body{padding:12px 15px;font-size:12.5px;line-height:1.75;color:var(--mu);white-space:pre-wrap;
  max-height:240px;overflow-y:auto}
.sources{display:flex;gap:7px;flex-wrap:wrap;margin-top:13px}
.src{display:inline-flex;align-items:center;gap:8px;padding:5px 11px;border-radius:999px;border:1px solid var(--bd);
  background:var(--s1);font-size:11.5px;color:var(--mu);text-decoration:none;max-width:260px;transition:all .2s}
.src:hover{background:var(--s2);color:var(--tx);border-color:var(--bd2)}
.src .s-n{font-family:var(--fm);font-size:9.5px;color:var(--ac);flex:none}
.src span:last-child{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.duel{display:grid;grid-template-columns:1fr 1fr;gap:12px}
.duel-pane{border:1px solid var(--bd);border-radius:var(--r2);background:var(--s1);display:flex;flex-direction:column;overflow:hidden}
.duel-head{display:flex;align-items:center;gap:8px;padding:9px 14px;border-bottom:1px solid var(--bd);
  font-family:var(--fm);font-size:10.5px;color:var(--ac)}
.duel-body{padding:14px 16px;font-size:13.5px;line-height:1.7;flex:1}
.duel-adopt{border:none;border-top:1px solid var(--bd);background:none;color:var(--mu);font-family:var(--f);
  font-size:12px;padding:10px;cursor:pointer;transition:all .18s}
.duel-adopt:hover{background:var(--s2);color:var(--tx)}
.thinking{display:flex;gap:5px;padding:4px 0}
.thinking span{width:5px;height:5px;border-radius:50%;background:var(--ac);animation:th 1.3s ease-in-out infinite}
.thinking span:nth-child(2){animation-delay:.18s}.thinking span:nth-child(3){animation-delay:.36s}
@keyframes th{0%,100%{opacity:.25}50%{opacity:1;transform:translateY(-3px)}}
/* ── COMPOSER · die Karte ── */
#dock{flex:none;padding:0 20px 18px;display:flex;justify-content:center;transition:padding .4s var(--e)}
#dock.center{position:absolute;left:0;right:0;bottom:auto;top:50%;transform:translateY(-8px);padding:0 20px}
#composer{width:100%;max-width:760px;background:rgba(20,20,20,.72);border:1px solid var(--bd);
  border-radius:var(--r);transition:border-color .25s,box-shadow .25s;
  -webkit-backdrop-filter:blur(28px) saturate(1.4);backdrop-filter:blur(28px) saturate(1.4);
  box-shadow:0 20px 60px -20px rgba(0,0,0,.9)}
#composer:focus-within{border-color:var(--bd2);box-shadow:0 0 0 3px rgba(201,169,98,.07),0 20px 60px -20px rgba(0,0,0,.9)}
#composer.dropping{border-color:var(--ac)}
#attachPreview{display:none;gap:8px;padding:13px 14px 0;flex-wrap:wrap}
#attachPreview .ap{position:relative}
#attachPreview img{width:52px;height:52px;object-fit:cover;border-radius:9px;border:1px solid var(--bd)}
#attachPreview .ap-x{position:absolute;top:-5px;right:-5px;width:17px;height:17px;border-radius:50%;
  border:1px solid var(--bd);background:#151515;color:var(--mu);font-size:10px;cursor:pointer;line-height:1}
#attachPreview .ap-x:hover{color:var(--dg)}
#input{width:100%;background:none;border:none;outline:none;resize:none;color:var(--tx);
  font-family:var(--f);font-size:15px;font-weight:400;line-height:1.6;padding:15px 18px 8px;max-height:210px;min-height:48px}
#input::placeholder{color:var(--fa)}
.cbar{display:flex;align-items:center;gap:6px;padding:6px 10px 10px}
.cbar .sp{flex:1}
.cb{display:flex;align-items:center;gap:7px;height:32px;padding:0 10px;border-radius:999px;border:1px solid transparent;
  background:none;color:var(--mu);cursor:pointer;font-family:var(--f);font-size:12.5px;transition:all .18s}
.cb:hover{background:var(--s2);color:var(--tx)}
.cb.on{background:var(--acs);border-color:rgba(201,169,98,.3);color:var(--ac2)}
.cb.icon{padding:0;width:32px;justify-content:center}
.cb .ico{width:16px;height:16px}
.cb.busy .ico{animation:sp 1s linear infinite}
@keyframes sp{to{transform:rotate(360deg)}}
.mic-live{color:var(--dg)!important;background:rgba(224,133,133,.1)!important}
#send{width:34px;height:34px;border-radius:50%;border:none;background:var(--tx);color:#000;cursor:pointer;
  display:grid;place-items:center;flex:none;transition:all .2s var(--e)}
#send:hover{transform:scale(1.06)}
#send:disabled{opacity:.28;cursor:default;transform:none}
#send .ico{width:16px;height:16px;stroke-width:2.2}
#send.stopping{background:var(--dg);color:#fff}
#tokEst{font-family:var(--fm);font-size:10px;color:var(--fa);padding:0 6px;opacity:0;transition:opacity .2s}
#tokEst.show{opacity:1}
/* Modus-Pillen unter dem Feld */
#modes{display:flex;align-items:center;gap:6px;padding:0 10px 10px;flex-wrap:wrap}
.mode{display:flex;align-items:center;gap:7px;height:29px;padding:0 12px;border-radius:999px;
  border:1px solid var(--bd);background:none;color:var(--mu);cursor:pointer;
  font-family:var(--f);font-size:12px;transition:all .18s var(--e)}
.mode:hover{background:var(--s2);color:var(--tx);border-color:var(--bd2)}
.mode.on{background:var(--acs);border-color:rgba(201,169,98,.34);color:var(--ac2)}
.mode em{font-style:normal;font-size:12.5px;line-height:1}
.mode.on em{color:var(--ac)}
.mode.more{color:var(--fa);padding:0 11px}
#modes .cnt{font-family:var(--fm);font-size:9.5px;color:var(--ac);background:var(--acs);
  border-radius:999px;padding:1px 6px;margin-left:1px}
#footHint{text-align:center;font-size:10.5px;color:var(--fa);margin-top:9px}
#jump{position:absolute;bottom:calc(100% + 12px);left:50%;transform:translateX(-50%);display:none;
  align-items:center;gap:7px;height:30px;padding:0 13px;border-radius:999px;border:1px solid var(--bd);
  background:rgba(20,20,20,.92);color:var(--mu);font-family:var(--f);font-size:12px;cursor:pointer;
  -webkit-backdrop-filter:blur(14px);backdrop-filter:blur(14px)}
#jump.show{display:inline-flex;animation:up .3s var(--e)}
#jump:hover{color:var(--tx)}
/* ── SHEET · Konsole ── */
.ov{position:fixed;inset:0;z-index:60;display:none;align-items:center;justify-content:center;padding:24px;
  background:rgba(0,0,0,.62);-webkit-backdrop-filter:blur(10px);backdrop-filter:blur(10px)}
.ov.open{display:flex;animation:fade .22s var(--e)}
@keyframes fade{from{opacity:0}to{opacity:1}}
.card{width:min(640px,100%);max-height:84vh;display:flex;flex-direction:column;
  background:rgba(17,17,17,.96);border:1px solid var(--bd);border-radius:var(--r);overflow:hidden;
  box-shadow:0 40px 100px -30px rgba(0,0,0,.95);animation:cin .3s var(--e)}
@keyframes cin{from{opacity:0;transform:translateY(16px) scale(.98)}to{opacity:1;transform:none}}
#ctabs{display:flex;gap:4px;padding:12px 12px 0;flex:none;overflow-x:auto;scrollbar-width:none}
#ctabs::-webkit-scrollbar{display:none}
.ctab{height:32px;padding:0 14px;border-radius:999px;border:none;background:none;color:var(--mu);
  font-family:var(--f);font-size:12.5px;cursor:pointer;transition:all .18s;white-space:nowrap}
.ctab:hover{background:var(--s2);color:var(--tx)}
.ctab.on{background:var(--s3);color:var(--tx)}
.cbody{flex:1;overflow-y:auto;padding:18px 20px 22px}
.cpage{display:none}.cpage.on{display:block;animation:up .3s var(--e)}
.grp{font-size:11px;letter-spacing:.1em;text-transform:uppercase;color:var(--fa);margin:22px 0 11px;font-weight:500}
.grp:first-child{margin-top:0}
.f{margin-bottom:13px}
.f label{display:block;font-size:12px;color:var(--mu);margin-bottom:7px}
.f input,.f textarea,.f select{width:100%;background:var(--s1);border:1px solid var(--bd);border-radius:var(--r3);
  color:var(--tx);font-family:var(--f);font-size:13.5px;padding:9px 12px;outline:none;transition:border-color .2s}
.f input:focus,.f textarea:focus,.f select:focus{border-color:var(--bd2)}
.f textarea{resize:vertical;min-height:62px;line-height:1.65}
.f textarea.mono,.f input.mono{font-family:var(--fm);font-size:12px}
.f select{appearance:none;cursor:pointer;
  background-image:url("data:i
